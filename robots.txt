User-agent: *
Allow: /

# Les avis de la page d'accueil et de /avis viennent de deux sources, toutes
# deux lues par le navigateur : Google doit pouvoir les atteindre, sinon la
# section Avis lui apparaît vide.
#   - la fonction ci-dessous pour les avis déposés par les clients
#   - le fichier data/reviews.json pour les avis historiques (lu par avis.js)
# La règle la plus précise l'emporte, donc cette autorisation passe avant le blocage.
Allow: /.netlify/functions/avis-liste

# Le reste des fonctions n'a rien à faire dans un index de moteur de recherche.
# (L'ancienne règle disait /netlify/ sans point : elle ne correspondait à rien.)
Disallow: /.netlify/

Sitemap: https://xperiencevision.com/sitemap.xml
